<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="post">
	<insert id="insert" parameterType="PostVO">
		
		<!-- <selectKey keyProperty="id" resultType="Long" order = "BEFORE">
			SELECT SEQ_POST.NEXTVAL FROM DUAL
		</selectKey> 사용하는게 좋다-->
	
	 	INSERT INTO TBL_POST
		VALUES(SEQ_POST.NEXTVAL, #{postTitle}, #{postContent}, CURRENT_TIMESTAMP)
	</insert>
	<select id="selectAll" resultType="PostVO">
		SELECT ID, POST_TITLE, POST_CONTENT, POST_CREATE_AT
		FROM TBL_POST
		ORDER BY ID DESC
	</select>
	<select id="select" parameterType="Long" resultType="PostVO">
		SELECT ID, POST_TITLE, POST_CONTENT, POST_CREATE_AT
		FROM TBL_POST
		WHERE ID = #{id}
	</select>
	<update id="update" parameterType="PostVO">
		UPDATE TBL_POST
		SET POST_TITLE = #{postTitle}, POST_CONTENT = #{postContent}, POST_CREATE_AT = CURRENT_TIMESTAMP
		WHERE ID = #{id}
	</update>
	<delete id="delete" parameterType="Long">
		DELETE FROM TBL_POST
		WHERE ID = #{ID}
	</delete>
</mapper>